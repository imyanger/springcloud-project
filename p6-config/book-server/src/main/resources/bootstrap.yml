# 配置中心config的配置，这个必须放在bootstrap.yml或bootstrap.properties中，否则无法读取
spring:
  cloud:
    config:
      uri: http://localhost:10850 #config的地址
      name: book-server #配置文件的前缀，当服务名与配置文件名前缀相同时，可不配置
      profile: dev #配置文件后缀，即请求文件为{name}-{profile}.yml